# Regex Data Extraction

# Project Overview
This project extracts structured data (emails, URLs, phone numbers, currency amounts, and time) from raw text using **Regular Expressions (Regex)**.

## Technologies Used
- **Python** (Regex module `re`)
- **Unittest** (for validation)
- **GitHub** (version control)


Extracts **Emails**, **URLs**, **Phone Numbers**, **Currency**, and **Time**  
Handles **multiple formats** and **edge cases**  
Includes **unit tests** for validation  
Easy-to-use **Python script**  

## Setup Instructions
1️**Clone the repository**  
```bash
git clone https://github.com/{YourUsername}/alu_regex-data-extraction-{YourUsername}.git
